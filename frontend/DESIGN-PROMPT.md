Update `DESIGN.md` so it matches the current project exactly. Inspect the entire codebase and treat the implementation as the source of truth.

Document only what exists today, including:

- Tech stack and UI libraries
- Design system and architecture
- Brand identity and visual style
- Color palette with semantic usage
- Typography, font sizes, weights, and line heights
- Spacing scale and layout system
- Border radius, shadows, and borders
- Components and their design patterns
- Icons, illustrations, and imagery
- Animations and transitions
- Responsive breakpoints and behavior
- Theme support, including light and dark mode
- Design tokens, constants, and reusable styles
- Accessibility patterns
- File structure related to styling and UI

Do not invent, assume, or recommend changes. Remove outdated information and rewrite sections where needed so `DESIGN.md` reflects the current implementation with clear, concise documentation.
